/**
 * Metadata keys. Used to access and store metadata.
 * Linked to a class or property.
 * @internal
 */
export enum MetadataKeys {
    module = "module",
    component = "component",
    events = "events",
    config = "config",
    subComponent = "sub-components"
}

/**
 * Global metadata keys. Used to access and store metadata.
 * @internal
 */
export enum GlobalMetadataKeys {
    providers = "providers"
}